include 'assets'
include 'common'
include 'p2p'
include 'core'
include 'desktop'

rootProject.name = 'bisq'

//rootProject.children.each { project ->
//    project.name = "${rootProject.name}-${project.name}"
//}
